<UserControl x:Class="YourNamespace.NodeEditorCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <ScrollViewer Name="CanvasScroller" 
                  HorizontalScrollBarVisibility="Auto" 
                  VerticalScrollBarVisibility="Auto"
                  PreviewMouseWheel="CanvasScroller_PreviewMouseWheel">
        <Grid>
            <Grid.LayoutTransform>
                <ScaleTransform x:Name="CanvasScale" ScaleX="1" ScaleY="1"/>
            </Grid.LayoutTransform>

            <!-- Grid lines canvas (background) -->
            <Canvas Name="GridCanvas" Width="2000" Height="2000"/>

            <!-- Main canvas for shapes (foreground) -->
            <Canvas Name="MainCanvas" 
                    Width="2000" 
                    Height="2000" 
                    Background="Transparent"
                    MouseLeftButtonDown="MainCanvas_MouseLeftButtonDown"/>
        </Grid>
    </ScrollViewer>
</UserControl>